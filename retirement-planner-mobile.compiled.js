//# planner-src-hash=43879394
// GENERATED by tools/build.cjs from retirement-planner-mobile.jsx — do not edit.
// Loaded only when the hash above matches the build stamp in the page (and,
// when served locally, the .jsx itself); otherwise the page recompiles.
"use strict";

function _slicedToArray(r, e) { return _arrayWithHoles(r) || _iterableToArrayLimit(r, e) || _unsupportedIterableToArray(r, e) || _nonIterableRest(); }
function _nonIterableRest() { throw new TypeError("Invalid attempt to destructure non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); }
function _iterableToArrayLimit(r, l) { var t = null == r ? null : "undefined" != typeof Symbol && r[Symbol.iterator] || r["@@iterator"]; if (null != t) { var e, n, i, u, a = [], f = !0, o = !1; try { if (i = (t = t.call(r)).next, 0 === l) { if (Object(t) !== t) return; f = !1; } else for (; !(f = (e = i.call(t)).done) && (a.push(e.value), a.length !== l); f = !0); } catch (r) { o = !0, n = r; } finally { try { if (!f && null != t["return"] && (u = t["return"](), Object(u) !== u)) return; } finally { if (o) throw n; } } return a; } }
function _arrayWithHoles(r) { if (Array.isArray(r)) return r; }
function _toConsumableArray(r) { return _arrayWithoutHoles(r) || _iterableToArray(r) || _unsupportedIterableToArray(r) || _nonIterableSpread(); }
function _nonIterableSpread() { throw new TypeError("Invalid attempt to spread non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); }
function _unsupportedIterableToArray(r, a) { if (r) { if ("string" == typeof r) return _arrayLikeToArray(r, a); var t = {}.toString.call(r).slice(8, -1); return "Object" === t && r.constructor && (t = r.constructor.name), "Map" === t || "Set" === t ? Array.from(r) : "Arguments" === t || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t) ? _arrayLikeToArray(r, a) : void 0; } }
function _iterableToArray(r) { if ("undefined" != typeof Symbol && null != r[Symbol.iterator] || null != r["@@iterator"]) return Array.from(r); }
function _arrayWithoutHoles(r) { if (Array.isArray(r)) return _arrayLikeToArray(r); }
function _arrayLikeToArray(r, a) { (null == a || a > r.length) && (a = r.length); for (var e = 0, n = Array(a); e < a; e++) n[e] = r[e]; return n; }
function ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
function _objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? ownKeys(Object(t), !0).forEach(function (r) { _defineProperty(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
function _defineProperty(e, r, t) { return (r = _toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
// ── shared calc engine (loaded via <script src="engine.js"> in mobile.html) ──
var PlannerEngine = typeof window !== 'undefined' && window.PlannerEngine || {};
var MAX_AGE = PlannerEngine.MAX_AGE,
  BROKERAGE_COST_BASIS_ESTIMATE = PlannerEngine.BROKERAGE_COST_BASIS_ESTIMATE,
  MAX_ITERATIONS_FOR_TAX_CALC = PlannerEngine.MAX_ITERATIONS_FOR_TAX_CALC,
  MONTE_CARLO_TAX_ESTIMATE = PlannerEngine.MONTE_CARLO_TAX_ESTIMATE,
  SAVE_DEBOUNCE_MS = PlannerEngine.SAVE_DEBOUNCE_MS,
  PRE_TAX_TYPES = PlannerEngine.PRE_TAX_TYPES,
  ROTH_TYPES = PlannerEngine.ROTH_TYPES,
  BROKERAGE_TYPES = PlannerEngine.BROKERAGE_TYPES,
  HSA_TYPES = PlannerEngine.HSA_TYPES,
  isPreTaxAccount = PlannerEngine.isPreTaxAccount,
  isRothAccount = PlannerEngine.isRothAccount,
  isBrokerageAccount = PlannerEngine.isBrokerageAccount,
  isHSAAccount = PlannerEngine.isHSAAccount,
  FEDERAL_TAX_BRACKETS_2026 = PlannerEngine.FEDERAL_TAX_BRACKETS_2026,
  STANDARD_DEDUCTION_2026 = PlannerEngine.STANDARD_DEDUCTION_2026,
  STATE_TAX_RATES = PlannerEngine.STATE_TAX_RATES,
  STATES_EXEMPT_RETIREMENT_INCOME = PlannerEngine.STATES_EXEMPT_RETIREMENT_INCOME,
  STATES_EXEMPT_ALL_RETIREMENT_DISTRIBUTIONS = PlannerEngine.STATES_EXEMPT_ALL_RETIREMENT_DISTRIBUTIONS,
  STATES_THAT_TAX_SS = PlannerEngine.STATES_THAT_TAX_SS,
  ALABAMA_TAX_BRACKETS = PlannerEngine.ALABAMA_TAX_BRACKETS,
  ALABAMA_PERSONAL_EXEMPTION = PlannerEngine.ALABAMA_PERSONAL_EXEMPTION,
  ALABAMA_OVER_65_RETIREMENT_EXCLUSION = PlannerEngine.ALABAMA_OVER_65_RETIREMENT_EXCLUSION,
  getAlabamaStandardDeduction = PlannerEngine.getAlabamaStandardDeduction,
  calculateAlabamaTax = PlannerEngine.calculateAlabamaTax,
  FICA_SS_RATE = PlannerEngine.FICA_SS_RATE,
  FICA_SS_WAGE_BASE_2025 = PlannerEngine.FICA_SS_WAGE_BASE_2025,
  FICA_MEDICARE_RATE = PlannerEngine.FICA_MEDICARE_RATE,
  FICA_ADDITIONAL_MEDICARE_RATE = PlannerEngine.FICA_ADDITIONAL_MEDICARE_RATE,
  FICA_ADDITIONAL_MEDICARE_THRESHOLD = PlannerEngine.FICA_ADDITIONAL_MEDICARE_THRESHOLD,
  calculateFICA = PlannerEngine.calculateFICA,
  RMD_FACTORS = PlannerEngine.RMD_FACTORS,
  IRMAA_THRESHOLDS_2025 = PlannerEngine.IRMAA_THRESHOLDS_2025,
  SS_FULL_RETIREMENT_AGE = PlannerEngine.SS_FULL_RETIREMENT_AGE,
  ACA_FPL_2025 = PlannerEngine.ACA_FPL_2025,
  QCD_ANNUAL_LIMIT = PlannerEngine.QCD_ANNUAL_LIMIT,
  QCD_START_AGE = PlannerEngine.QCD_START_AGE,
  SS_EARNINGS_TEST_LIMIT_2025 = PlannerEngine.SS_EARNINGS_TEST_LIMIT_2025,
  SS_EARNINGS_TEST_FRA_LIMIT_2025 = PlannerEngine.SS_EARNINGS_TEST_FRA_LIMIT_2025,
  MEDICARE_PART_B_STANDARD_2025 = PlannerEngine.MEDICARE_PART_B_STANDARD_2025,
  calculateIRMAA = PlannerEngine.calculateIRMAA,
  calculateIRMAASurcharge = PlannerEngine.calculateIRMAASurcharge,
  calculateSSEarningsTestReduction = PlannerEngine.calculateSSEarningsTestReduction,
  calculateSSBenefit = PlannerEngine.calculateSSBenefit,
  getRmdStartAge = PlannerEngine.getRmdStartAge,
  getDefaultRothConversionWindow = PlannerEngine.getDefaultRothConversionWindow,
  calculateACASubsidy = PlannerEngine.calculateACASubsidy,
  calculateHealthcareExpenses = PlannerEngine.calculateHealthcareExpenses,
  calculateRecurringExpenses = PlannerEngine.calculateRecurringExpenses,
  MEDICARE_PART_B_PREMIUM_2025 = PlannerEngine.MEDICARE_PART_B_PREMIUM_2025,
  MEDICARE_PART_D_PREMIUM_2025 = PlannerEngine.MEDICARE_PART_D_PREMIUM_2025,
  MEDICARE_SUPPLEMENT_PREMIUM_2025 = PlannerEngine.MEDICARE_SUPPLEMENT_PREMIUM_2025,
  MEDICARE_OOP_ANNUAL_2025 = PlannerEngine.MEDICARE_OOP_ANNUAL_2025,
  PRE_65_HEALTHCARE_ANNUAL_2025 = PlannerEngine.PRE_65_HEALTHCARE_ANNUAL_2025,
  MEDICAL_INFLATION_RATE = PlannerEngine.MEDICAL_INFLATION_RATE,
  LTC_MONTHLY_ASSISTED_LIVING_2025 = PlannerEngine.LTC_MONTHLY_ASSISTED_LIVING_2025,
  LTC_DEFAULT_DURATION_MONTHS = PlannerEngine.LTC_DEFAULT_DURATION_MONTHS,
  calculateFederalTax = PlannerEngine.calculateFederalTax,
  calculateStateTax = PlannerEngine.calculateStateTax,
  calculateRMD = PlannerEngine.calculateRMD,
  calculateSocialSecurityTaxableAmount = PlannerEngine.calculateSocialSecurityTaxableAmount,
  CAPITAL_GAINS_THRESHOLDS_2025 = PlannerEngine.CAPITAL_GAINS_THRESHOLDS_2025,
  calculateCapitalGainsTax = PlannerEngine.calculateCapitalGainsTax,
  calculateNIIT = PlannerEngine.calculateNIIT,
  computeProjections = PlannerEngine.computeProjections,
  planShortfall = PlannerEngine.planShortfall,
  breakingPoint = PlannerEngine.breakingPoint,
  STRESS_DIMENSIONS = PlannerEngine.STRESS_DIMENSIONS;

// React globals are provided by mobile.html
// Do not add import statements - this file runs in browser via Babel transform

// ============================================================================
// MOBILE PLANNER
// ============================================================================
// A standalone, simplified retirement planner for mobile: you enter your own
// numbers here, and they stay on the phone.
//
// Design philosophy:
//   - One screen of inputs, results update live underneath
//   - Use native HTML range sliders (great mobile UX, big touch targets)
//   - Single filer only; couples can mentally adjust by halving spending
//   - One synthetic portfolio account + one SS stream feeds the FULL engine
//   - Inputs auto-save to localStorage under a mobile-specific key, so a
//     refresh restores them (see LOCAL PERSISTENCE below). This line used to say
//     the opposite; persistence was added later and the claim was left behind.
//
// This file does NOT import or share state with the desktop version.
// They live side-by-side as independent apps at separate URLs.
// ============================================================================

// ============================================
// CONSTANTS - Extracted magic numbers and repeated arrays
// ============================================

// ── LOCAL PERSISTENCE ───────────────────────────────────────────────────────
// The mobile app auto-saves your inputs to localStorage and restores them on the next visit,
// so you don't have to re-enter parameters every time. Stored under a mobile-specific key
// (separate from the desktop planner's data). Best-effort: silently no-ops if storage is
// unavailable (e.g. Safari Private Mode) or the data can't be parsed.
var MOBILE_STORAGE_KEY = 'retirementWhatIf_mobile_v1';

// Schema version for the persisted mobile state. Bump and add a migration
// entry whenever the saved shape changes incompatibly.
var MOBILE_SCHEMA_VERSION = 1;
var mobileMigrations = {
  // Example: 2: (data) => ({ ...data, newField: defaultValue }),
};
var isQuotaError = function isQuotaError(e) {
  return !!e && (e.name === 'QuotaExceededError' || e.name === 'NS_ERROR_DOM_QUOTA_REACHED' || e.code === 22 || e.code === 1014);
};
var loadMobileState = function loadMobileState() {
  try {
    var raw = localStorage.getItem(MOBILE_STORAGE_KEY);
    if (!raw) return {};
    var data = JSON.parse(raw);
    var version = data && typeof data.schemaVersion === 'number' ? data.schemaVersion : 0;
    while (version < MOBILE_SCHEMA_VERSION) {
      var next = version + 1;
      var migrate = mobileMigrations[next];
      if (!migrate) break;
      data = migrate(data);
      version = next;
    }
    if (data && _typeof(data) === 'object') data.schemaVersion = MOBILE_SCHEMA_VERSION;
    return data || {};
  } catch (e) {
    return {};
  }
};

// Returns {ok, quota} so callers can surface failures (e.g. quota exhaustion)
// instead of silently losing writes.
var saveMobileState = function saveMobileState(state) {
  try {
    var payload = state && _typeof(state) === 'object' ? _objectSpread(_objectSpread({}, state), {}, {
      schemaVersion: MOBILE_SCHEMA_VERSION
    }) : state;
    localStorage.setItem(MOBILE_STORAGE_KEY, JSON.stringify(payload));
    return {
      ok: true
    };
  } catch (e) {
    return {
      ok: false,
      quota: isQuotaError(e)
    };
  }
};

// ============================================================================
// MOBILE UI
// ============================================================================

// Formatters tailored for mobile (shorter strings, larger units)
var fmt = function fmt(n) {
  if (n == null || isNaN(n)) return '—';
  if (Math.abs(n) >= 1e6) return '$' + (n / 1e6).toFixed(2) + 'M';
  if (Math.abs(n) >= 1e3) return '$' + (n / 1e3).toFixed(0) + 'K';
  return '$' + Math.round(n).toLocaleString();
};
var fmtFull = function fmtFull(n) {
  if (n == null || isNaN(n)) return '—';
  return '$' + Math.round(n).toLocaleString();
};

// A labeled slider row. Mobile-friendly: large hit target, value badge on the right.
function SliderRow(_ref) {
  var label = _ref.label,
    value = _ref.value,
    _onChange = _ref.onChange,
    min = _ref.min,
    max = _ref.max,
    _ref$step = _ref.step,
    step = _ref$step === void 0 ? 1 : _ref$step,
    format = _ref.format;
  var displayValue = format ? format(value) : value;
  return /*#__PURE__*/React.createElement("div", {
    className: "py-2"
  }, /*#__PURE__*/React.createElement("div", {
    className: "flex items-baseline justify-between mb-1.5"
  }, /*#__PURE__*/React.createElement("span", {
    className: "text-sm text-slate-300 font-medium"
  }, label), /*#__PURE__*/React.createElement("span", {
    className: "text-base text-emerald-400 font-semibold tabular-nums"
  }, displayValue)), /*#__PURE__*/React.createElement("input", {
    type: "range",
    min: min,
    max: max,
    step: step,
    value: value,
    onChange: function onChange(e) {
      return _onChange(Number(e.target.value));
    },
    className: "w-full h-3 rounded-lg appearance-none cursor-pointer bg-slate-700 accent-emerald-500",
    style: {
      touchAction: 'manipulation'
    }
  }));
}

// A labeled number input for amounts that need precise typing.
function MoneyInput(_ref2) {
  var label = _ref2.label,
    value = _ref2.value,
    _onChange2 = _ref2.onChange,
    hint = _ref2.hint;
  return /*#__PURE__*/React.createElement("div", {
    className: "py-2"
  }, /*#__PURE__*/React.createElement("label", {
    className: "block text-sm text-slate-300 font-medium mb-1"
  }, label), /*#__PURE__*/React.createElement("div", {
    className: "relative"
  }, /*#__PURE__*/React.createElement("span", {
    className: "absolute left-3 top-1/2 -translate-y-1/2 text-slate-500"
  }, "$"), /*#__PURE__*/React.createElement("input", {
    type: "number",
    inputMode: "decimal",
    value: value === 0 ? '' : value,
    onChange: function onChange(e) {
      return _onChange2(Number(e.target.value) || 0);
    },
    onFocus: function onFocus(e) {
      return e.target.select();
    },
    placeholder: "0",
    className: "w-full pl-7 pr-3 py-2.5 bg-slate-800 border border-slate-700 rounded-lg text-base text-slate-100 focus:outline-none focus:border-emerald-500"
  })), hint && /*#__PURE__*/React.createElement("p", {
    className: "text-xs text-slate-500 mt-1"
  }, hint));
}

// A compact result card. Big number, label below.
function ResultCard(_ref3) {
  var label = _ref3.label,
    value = _ref3.value,
    sublabel = _ref3.sublabel,
    _ref3$color = _ref3.color,
    color = _ref3$color === void 0 ? 'emerald' : _ref3$color;
  var colorClass = {
    emerald: 'text-emerald-400',
    amber: 'text-amber-400',
    sky: 'text-sky-400',
    red: 'text-red-400',
    purple: 'text-purple-400'
  }[color] || 'text-emerald-400';
  return /*#__PURE__*/React.createElement("div", {
    className: "bg-slate-800/60 border border-slate-700 rounded-lg p-3"
  }, /*#__PURE__*/React.createElement("div", {
    className: "text-xs text-slate-400 uppercase tracking-wide"
  }, label), /*#__PURE__*/React.createElement("div", {
    className: "text-xl font-bold ".concat(colorClass, " tabular-nums mt-0.5")
  }, value), sublabel && /*#__PURE__*/React.createElement("div", {
    className: "text-xs text-slate-500 mt-0.5"
  }, sublabel));
}

// Tiny inline sparkline showing portfolio over time.
// Uses SVG, no external library. Renders a smooth filled area.
function Sparkline(_ref4) {
  var data = _ref4.data,
    retirementAge = _ref4.retirementAge;
  if (!data || data.length === 0) return null;
  var width = 320;
  var height = 80;
  var padding = 4;
  var values = data.map(function (d) {
    return d.totalPortfolio;
  });
  var ages = data.map(function (d) {
    return d.myAge;
  });
  var maxV = Math.max.apply(Math, _toConsumableArray(values).concat([1]));
  var minV = 0;
  var minAge = Math.min.apply(Math, _toConsumableArray(ages));
  var maxAge = Math.max.apply(Math, _toConsumableArray(ages));
  var xOf = function xOf(age) {
    return padding + (age - minAge) / (maxAge - minAge) * (width - 2 * padding);
  };
  var yOf = function yOf(v) {
    return padding + (1 - (v - minV) / (maxV - minV)) * (height - 2 * padding);
  };
  var linePath = data.map(function (d, i) {
    return "".concat(i === 0 ? 'M' : 'L', " ").concat(xOf(d.myAge).toFixed(1), " ").concat(yOf(d.totalPortfolio).toFixed(1));
  }).join(' ');
  var fillPath = "".concat(linePath, " L ").concat(xOf(maxAge).toFixed(1), " ").concat(height - padding, " L ").concat(xOf(minAge).toFixed(1), " ").concat(height - padding, " Z");
  var retirementX = xOf(retirementAge);
  return /*#__PURE__*/React.createElement("svg", {
    width: "100%",
    height: height,
    viewBox: "0 0 ".concat(width, " ").concat(height),
    preserveAspectRatio: "none",
    className: "block"
  }, /*#__PURE__*/React.createElement("defs", null, /*#__PURE__*/React.createElement("linearGradient", {
    id: "sparkFill",
    x1: "0",
    y1: "0",
    x2: "0",
    y2: "1"
  }, /*#__PURE__*/React.createElement("stop", {
    offset: "0%",
    stopColor: "#10b981",
    stopOpacity: "0.6"
  }), /*#__PURE__*/React.createElement("stop", {
    offset: "100%",
    stopColor: "#10b981",
    stopOpacity: "0"
  }))), /*#__PURE__*/React.createElement("path", {
    d: fillPath,
    fill: "url(#sparkFill)"
  }), /*#__PURE__*/React.createElement("path", {
    d: linePath,
    fill: "none",
    stroke: "#10b981",
    strokeWidth: "2"
  }), /*#__PURE__*/React.createElement("line", {
    x1: retirementX,
    y1: padding,
    x2: retirementX,
    y2: height - padding,
    stroke: "#fbbf24",
    strokeWidth: "1",
    strokeDasharray: "3 3",
    opacity: "0.7"
  }));
}

// A collapsible income stream section. Header shows the stream name + its current annual
// contribution. When the toggle is off, the body is hidden — keeping the inputs panel short
// for users who only have one or two streams. When on, shows amount, start age, and COLA toggle.
//
// Layout note: each section is its own visually distinct block (subtle border, label color).
// Three of these stacked vertically is about 2/3 of a screen when all enabled, less when off.
function IncomeStreamSection(_ref5) {
  var title = _ref5.title,
    accentColor = _ref5.accentColor,
    enabled = _ref5.enabled,
    onToggle = _ref5.onToggle,
    annualPreview = _ref5.annualPreview,
    children = _ref5.children;
  var accentClass = {
    sky: 'border-sky-700/40 bg-sky-900/10',
    violet: 'border-violet-700/40 bg-violet-900/10',
    cyan: 'border-cyan-700/40 bg-cyan-900/10'
  }[accentColor] || 'border-slate-700 bg-slate-800/40';
  var labelClass = {
    sky: 'text-sky-300',
    violet: 'text-violet-300',
    cyan: 'text-cyan-300'
  }[accentColor] || 'text-slate-300';
  return /*#__PURE__*/React.createElement("div", {
    className: "rounded-lg border ".concat(accentClass, " p-3 mb-2")
  }, /*#__PURE__*/React.createElement("div", {
    className: "flex items-center justify-between"
  }, /*#__PURE__*/React.createElement("div", {
    className: "flex items-center gap-2"
  }, /*#__PURE__*/React.createElement("button", {
    onClick: function onClick() {
      return onToggle(!enabled);
    },
    role: "switch",
    "aria-checked": enabled,
    className: "relative inline-flex h-6 w-11 items-center rounded-full transition-colors ".concat(enabled ? 'bg-emerald-600' : 'bg-slate-700')
  }, /*#__PURE__*/React.createElement("span", {
    className: "inline-block h-5 w-5 transform rounded-full bg-white transition-transform ".concat(enabled ? 'translate-x-5' : 'translate-x-0.5')
  })), /*#__PURE__*/React.createElement("span", {
    className: "text-sm font-semibold ".concat(labelClass)
  }, title)), enabled && annualPreview != null && /*#__PURE__*/React.createElement("span", {
    className: "text-sm text-emerald-400 font-medium tabular-nums"
  }, annualPreview > 0 ? "$".concat(Math.round(annualPreview).toLocaleString(), "/yr") : '—')), enabled && /*#__PURE__*/React.createElement("div", {
    className: "mt-2 pt-2 border-t border-slate-700/40"
  }, children));
}

// A simple toggle row used inside an IncomeStreamSection (for the COLA toggle).
// Compact, designed to fit alongside other compact controls.
// ── WHAT BREAKS FIRST ───────────────────────────────────────────────────────
// The desktop app carries three long-form reports. Only this one ports: the
// other two are about inputs mobile does not collect. The Widow's Year is the
// tax cliff a surviving spouse walks into when filing turns from joint to
// single, and mobile is single-filer with no spouse in the model at all — there
// is no transition to report on. The Roth Roadmap is a schedule for moving money
// from pre-tax to Roth, and mobile has one blended portfolio rather than the
// bucket split that report is entirely about. Rendering either here would mean
// inventing a household the user never entered.
//
// What Breaks First needs only a projection and the levers mobile already has,
// so it ports honestly — and it is the one that answers the question people
// actually pull out a phone to ask.
//
// The edges come from the engine's own bisection, so a number here and the same
// number on desktop are produced by the same code rather than by two
// implementations that agree until they don't.
function BreakingPointPanel(_ref6) {
  var ctx = _ref6.ctx,
    projections = _ref6.projections,
    retirementAge = _ref6.retirementAge;
  var _useState = useState(false),
    _useState2 = _slicedToArray(_useState, 2),
    open = _useState2[0],
    setOpen = _useState2[1];
  var _useState3 = useState(null),
    _useState4 = _slicedToArray(_useState3, 2),
    edges = _useState4[0],
    setEdges = _useState4[1];
  var _useState5 = useState(false),
    _useState6 = _slicedToArray(_useState5, 2),
    busy = _useState6[0],
    setBusy = _useState6[1];
  var ledger = useMemo(function () {
    return planShortfall ? planShortfall(projections, {
      retirementAge: retirementAge
    }) : null;
  }, [projections, retirementAge]);

  // Three bisections is roughly fifty projections. Fast on a laptop, not
  // necessarily on a phone, so it runs on demand rather than on every keystroke —
  // and the inputs above change constantly on a slider-driven UI.
  var findEdges = function findEdges() {
    if (!breakingPoint) return;
    setBusy(true);
    setTimeout(function () {
      try {
        setEdges({
          returnDrop: breakingPoint(ctx, 'returnDrop'),
          inflation: breakingPoint(ctx, 'inflation'),
          spending: breakingPoint(ctx, 'spending')
        });
      } catch (e) {
        setEdges({
          error: e.message
        });
      }
      setBusy(false);
    }, 30);
  };
  if (!ledger) return null;
  var fmt = function fmt(n) {
    return '$' + Math.round(n || 0).toLocaleString('en-US');
  };
  var survives = !ledger.fails;
  return /*#__PURE__*/React.createElement("div", {
    className: "mt-3"
  }, /*#__PURE__*/React.createElement("button", {
    onClick: function onClick() {
      setOpen(!open);
      if (!open && !edges) findEdges();
    },
    className: "w-full py-2.5 text-sm text-slate-400 border border-slate-700 rounded-lg active:bg-slate-800 transition-colors"
  }, open ? '▲ Hide what breaks first' : '▼ What breaks first?'), open && /*#__PURE__*/React.createElement("div", {
    className: "mt-3 space-y-3 text-sm bg-slate-800/40 border border-slate-700 rounded-lg p-3"
  }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: survives ? 'text-emerald-300 font-semibold' : 'text-red-300 font-semibold'
  }, survives ? 'The plan funds every year' : "Short in ".concat(ledger.shortYearCount, " year").concat(ledger.shortYearCount === 1 ? '' : 's')), !survives && ledger.firstShortYear && /*#__PURE__*/React.createElement("div", {
    className: "text-slate-400 text-xs mt-1"
  }, "First shortfall at age ", ledger.firstShortYear.myAge, ledger.worstYear && /*#__PURE__*/React.createElement(React.Fragment, null, " \xB7 worst is ", fmt(ledger.worstYear.unfundedShortfall), " at age ", ledger.worstYear.myAge)), !survives && ledger.recovers && /*#__PURE__*/React.createElement("div", {
    className: "text-amber-300 text-xs mt-1"
  }, "It recovers \u2014 income starting later covers spending from age ", (ledger.lastShortYear || {}).myAge + 1, " on."), survives && ledger.depletedYear && /*#__PURE__*/React.createElement("div", {
    className: "text-amber-300 text-xs mt-1"
  }, "Every year is funded, but the portfolio hits zero at age ", ledger.depletedYear.myAge, " and income carries it from there.")), /*#__PURE__*/React.createElement("div", {
    className: "border-t border-slate-700 pt-3"
  }, /*#__PURE__*/React.createElement("div", {
    className: "text-slate-300 font-medium mb-1"
  }, "How much room is there?"), /*#__PURE__*/React.createElement("p", {
    className: "text-[11px] text-slate-500 mb-2 leading-relaxed"
  }, "Each row is the value at which this plan gives way, with everything else held where it is \u2014 not a guess at what is likely."), busy && /*#__PURE__*/React.createElement("div", {
    className: "text-slate-500 text-xs"
  }, "Finding the edges\u2026"), edges && edges.error && /*#__PURE__*/React.createElement("div", {
    className: "text-red-300 text-xs"
  }, "Could not compute: ", edges.error), edges && !edges.error && /*#__PURE__*/React.createElement("div", {
    className: "space-y-2"
  }, /*#__PURE__*/React.createElement(EdgeRow, {
    label: "Return shortfall it survives",
    edge: edges.returnDrop,
    format: function format(v) {
      return "".concat((v * 100).toFixed(1), " pts below ").concat('' + (ctx.accts[0] ? (ctx.accts[0].cagr * 100).toFixed(1) : '?'), "%");
    }
  }), /*#__PURE__*/React.createElement(EdgeRow, {
    label: "Inflation it survives",
    edge: edges.inflation,
    format: function format(v) {
      return "".concat((v * 100).toFixed(1), "%");
    }
  }), /*#__PURE__*/React.createElement(EdgeRow, {
    label: "Spending it supports",
    edge: edges.spending,
    format: function format(v) {
      return fmt(v) + '/yr';
    }
  })))));
}

// One edge, and it has three outcomes rather than two — printing a number for all
// of them would be wrong in two of the three cases.
//
//   alreadyFails  — the plan is short as it stands, so there is no cushion to
//                   measure. Reporting the range's endpoint here would show a
//                   comfortable margin for a plan that is already failing.
//   survivesRange — nothing inside the tested range breaks it. That is a real
//                   answer, not a missing one.
//   otherwise     — `value` is the last value that survives, stated
//                   conservatively by the engine's bisection.
function EdgeRow(_ref7) {
  var label = _ref7.label,
    edge = _ref7.edge,
    format = _ref7.format;
  if (!edge) return null;
  return /*#__PURE__*/React.createElement("div", {
    className: "flex justify-between items-baseline gap-3"
  }, /*#__PURE__*/React.createElement("span", {
    className: "text-slate-400 text-xs"
  }, label), /*#__PURE__*/React.createElement("span", {
    className: "tabular-nums text-xs text-right"
  }, edge.alreadyFails ? /*#__PURE__*/React.createElement("span", {
    className: "text-red-300"
  }, "already short") : edge.survivesRange ? /*#__PURE__*/React.createElement("span", {
    className: "text-emerald-300"
  }, "holds throughout") : /*#__PURE__*/React.createElement("span", {
    className: "text-slate-200"
  }, format(edge.value))));
}
function ToggleRow(_ref8) {
  var label = _ref8.label,
    value = _ref8.value,
    onChange = _ref8.onChange,
    hint = _ref8.hint;
  return /*#__PURE__*/React.createElement("div", {
    className: "flex items-center justify-between py-1.5"
  }, /*#__PURE__*/React.createElement("div", {
    className: "flex-1 pr-3"
  }, /*#__PURE__*/React.createElement("div", {
    className: "text-sm text-slate-300"
  }, label), hint && /*#__PURE__*/React.createElement("div", {
    className: "text-[11px] text-slate-500"
  }, hint)), /*#__PURE__*/React.createElement("button", {
    onClick: function onClick() {
      return onChange(!value);
    },
    role: "switch",
    "aria-checked": value,
    className: "relative inline-flex h-5 w-9 items-center rounded-full transition-colors flex-shrink-0 ".concat(value ? 'bg-emerald-600' : 'bg-slate-600')
  }, /*#__PURE__*/React.createElement("span", {
    className: "inline-block h-4 w-4 transform rounded-full bg-white transition-transform ".concat(value ? 'translate-x-4' : 'translate-x-0.5')
  })));
}

// ── ONE SCREEN ──────────────────────────────────────────────────────────────
// Until v2.55.0 the phone had two views: this one, and a "My plan" view that
// read the plan saved by the full version in the same browser. But a phone
// keeps its own storage, so on a phone that plan was usually not the reader's
// at all — the full version saves a sample plan the moment it is first opened,
// and the "Switch to full desktop version" link below opens it — while the view
// that could actually hold someone's numbers was labelled a "quick what-if".
// There is one view now, and the numbers in it are the reader's.

// The example a first visit opens on — one table, so the screen can tell
// whether the numbers on it are still the example or are the reader's.
var MOBILE_DEFAULTS = {
  currentAge: 45,
  retirementAge: 65,
  legacyAge: 90,
  portfolio: 250000,
  annualContribution: 20000,
  contributionMode: 'fixed',
  currentSalary: 80000,
  contributionPercent: 10,
  desiredSpending: 60000,
  cagr: 7,
  inflationRate: 3,
  ssEnabled: true,
  ssMonthly: 2500,
  ssClaimAge: 67,
  ssCola: true,
  pensionEnabled: false,
  pensionAnnual: 0,
  pensionStartAge: 65,
  pensionCola: false,
  otherEnabled: false,
  otherAnnual: 0,
  otherStartAge: 65,
  otherEndAge: 90,
  otherCola: true
};
function MobilePlanner() {
  var _saved$currentAge, _saved$retirementAge, _saved$legacyAge, _saved$portfolio, _saved$annualContribu, _saved$contributionMo, _saved$currentSalary, _saved$contributionPe, _saved$desiredSpendin, _saved$cagr, _saved$inflationRate, _saved$ssEnabled, _saved$ssMonthly, _saved$ssClaimAge, _saved$ssCola, _saved$pensionEnabled, _saved$pensionAnnual, _saved$pensionStartAg, _saved$pensionCola, _saved$otherEnabled, _saved$otherAnnual, _saved$otherStartAge, _saved$otherEndAge, _saved$otherCola;
  // === Inputs (state) ===
  // Restore saved inputs once (synchronous localStorage read) and use each as the initial
  // value, falling back to the default when a field isn't present in saved data.
  var saved = useMemo(function () {
    return loadMobileState();
  }, []);
  var _useState7 = useState((_saved$currentAge = saved.currentAge) !== null && _saved$currentAge !== void 0 ? _saved$currentAge : MOBILE_DEFAULTS.currentAge),
    _useState8 = _slicedToArray(_useState7, 2),
    currentAge = _useState8[0],
    setCurrentAge = _useState8[1];
  var _useState9 = useState((_saved$retirementAge = saved.retirementAge) !== null && _saved$retirementAge !== void 0 ? _saved$retirementAge : MOBILE_DEFAULTS.retirementAge),
    _useState0 = _slicedToArray(_useState9, 2),
    retirementAge = _useState0[0],
    setRetirementAge = _useState0[1];
  var _useState1 = useState((_saved$legacyAge = saved.legacyAge) !== null && _saved$legacyAge !== void 0 ? _saved$legacyAge : MOBILE_DEFAULTS.legacyAge),
    _useState10 = _slicedToArray(_useState1, 2),
    legacyAge = _useState10[0],
    setLegacyAge = _useState10[1];
  var _useState11 = useState((_saved$portfolio = saved.portfolio) !== null && _saved$portfolio !== void 0 ? _saved$portfolio : MOBILE_DEFAULTS.portfolio),
    _useState12 = _slicedToArray(_useState11, 2),
    portfolio = _useState12[0],
    setPortfolio = _useState12[1];
  var _useState13 = useState((_saved$annualContribu = saved.annualContribution) !== null && _saved$annualContribu !== void 0 ? _saved$annualContribu : MOBILE_DEFAULTS.annualContribution),
    _useState14 = _slicedToArray(_useState13, 2),
    annualContribution = _useState14[0],
    setAnnualContribution = _useState14[1];
  // Percent-of-salary contribution mode. Missing in saved → 'fixed' for backwards-compat.
  // In 'percent' mode, the engine reads myEarnedIncome from the salary stream we inject below
  // and computes contribution = salary × (contributionPercent / 100) each year, so the
  // contribution automatically tracks salary COLA instead of silently shrinking in real terms.
  var _useState15 = useState((_saved$contributionMo = saved.contributionMode) !== null && _saved$contributionMo !== void 0 ? _saved$contributionMo : MOBILE_DEFAULTS.contributionMode),
    _useState16 = _slicedToArray(_useState15, 2),
    contributionMode = _useState16[0],
    setContributionMode = _useState16[1];
  var _useState17 = useState((_saved$currentSalary = saved.currentSalary) !== null && _saved$currentSalary !== void 0 ? _saved$currentSalary : MOBILE_DEFAULTS.currentSalary),
    _useState18 = _slicedToArray(_useState17, 2),
    currentSalary = _useState18[0],
    setCurrentSalary = _useState18[1];
  var _useState19 = useState((_saved$contributionPe = saved.contributionPercent) !== null && _saved$contributionPe !== void 0 ? _saved$contributionPe : MOBILE_DEFAULTS.contributionPercent),
    _useState20 = _slicedToArray(_useState19, 2),
    contributionPercent = _useState20[0],
    setContributionPercent = _useState20[1];
  var _useState21 = useState((_saved$desiredSpendin = saved.desiredSpending) !== null && _saved$desiredSpendin !== void 0 ? _saved$desiredSpendin : MOBILE_DEFAULTS.desiredSpending),
    _useState22 = _slicedToArray(_useState21, 2),
    desiredSpending = _useState22[0],
    setDesiredSpending = _useState22[1];
  var _useState23 = useState((_saved$cagr = saved.cagr) !== null && _saved$cagr !== void 0 ? _saved$cagr : MOBILE_DEFAULTS.cagr),
    _useState24 = _slicedToArray(_useState23, 2),
    cagr = _useState24[0],
    setCagr = _useState24[1];
  var _useState25 = useState((_saved$inflationRate = saved.inflationRate) !== null && _saved$inflationRate !== void 0 ? _saved$inflationRate : MOBILE_DEFAULTS.inflationRate),
    _useState26 = _slicedToArray(_useState25, 2),
    inflationRate = _useState26[0],
    setInflationRate = _useState26[1];

  // Three income streams: Social Security, Pension, Other.
  // Each has: enabled flag (so user can zero one out cleanly), amount, start age, and inflation toggle.
  // - SS uses PIA (monthly benefit at FRA) since that's what ssa.gov gives you.
  // - Pension and Other use direct annual amounts.
  // - Defaults reflect typical real-world behavior:
  //     SS: COLA on (Social Security has annual COLA)
  //     Pension: COLA off (most private pensions are not inflation-adjusted)
  //     Other: COLA on (rentals, royalties, part-time work roughly track inflation)
  var _useState27 = useState((_saved$ssEnabled = saved.ssEnabled) !== null && _saved$ssEnabled !== void 0 ? _saved$ssEnabled : MOBILE_DEFAULTS.ssEnabled),
    _useState28 = _slicedToArray(_useState27, 2),
    ssEnabled = _useState28[0],
    setSsEnabled = _useState28[1];
  var _useState29 = useState((_saved$ssMonthly = saved.ssMonthly) !== null && _saved$ssMonthly !== void 0 ? _saved$ssMonthly : MOBILE_DEFAULTS.ssMonthly),
    _useState30 = _slicedToArray(_useState29, 2),
    ssMonthly = _useState30[0],
    setSsMonthly = _useState30[1];
  var _useState31 = useState((_saved$ssClaimAge = saved.ssClaimAge) !== null && _saved$ssClaimAge !== void 0 ? _saved$ssClaimAge : MOBILE_DEFAULTS.ssClaimAge),
    _useState32 = _slicedToArray(_useState31, 2),
    ssClaimAge = _useState32[0],
    setSsClaimAge = _useState32[1];
  var _useState33 = useState((_saved$ssCola = saved.ssCola) !== null && _saved$ssCola !== void 0 ? _saved$ssCola : MOBILE_DEFAULTS.ssCola),
    _useState34 = _slicedToArray(_useState33, 2),
    ssCola = _useState34[0],
    setSsCola = _useState34[1];
  var _useState35 = useState((_saved$pensionEnabled = saved.pensionEnabled) !== null && _saved$pensionEnabled !== void 0 ? _saved$pensionEnabled : MOBILE_DEFAULTS.pensionEnabled),
    _useState36 = _slicedToArray(_useState35, 2),
    pensionEnabled = _useState36[0],
    setPensionEnabled = _useState36[1];
  var _useState37 = useState((_saved$pensionAnnual = saved.pensionAnnual) !== null && _saved$pensionAnnual !== void 0 ? _saved$pensionAnnual : MOBILE_DEFAULTS.pensionAnnual),
    _useState38 = _slicedToArray(_useState37, 2),
    pensionAnnual = _useState38[0],
    setPensionAnnual = _useState38[1];
  var _useState39 = useState((_saved$pensionStartAg = saved.pensionStartAge) !== null && _saved$pensionStartAg !== void 0 ? _saved$pensionStartAg : MOBILE_DEFAULTS.pensionStartAge),
    _useState40 = _slicedToArray(_useState39, 2),
    pensionStartAge = _useState40[0],
    setPensionStartAge = _useState40[1];
  var _useState41 = useState((_saved$pensionCola = saved.pensionCola) !== null && _saved$pensionCola !== void 0 ? _saved$pensionCola : MOBILE_DEFAULTS.pensionCola),
    _useState42 = _slicedToArray(_useState41, 2),
    pensionCola = _useState42[0],
    setPensionCola = _useState42[1];
  var _useState43 = useState((_saved$otherEnabled = saved.otherEnabled) !== null && _saved$otherEnabled !== void 0 ? _saved$otherEnabled : MOBILE_DEFAULTS.otherEnabled),
    _useState44 = _slicedToArray(_useState43, 2),
    otherEnabled = _useState44[0],
    setOtherEnabled = _useState44[1];
  var _useState45 = useState((_saved$otherAnnual = saved.otherAnnual) !== null && _saved$otherAnnual !== void 0 ? _saved$otherAnnual : MOBILE_DEFAULTS.otherAnnual),
    _useState46 = _slicedToArray(_useState45, 2),
    otherAnnual = _useState46[0],
    setOtherAnnual = _useState46[1];
  var _useState47 = useState((_saved$otherStartAge = saved.otherStartAge) !== null && _saved$otherStartAge !== void 0 ? _saved$otherStartAge : MOBILE_DEFAULTS.otherStartAge),
    _useState48 = _slicedToArray(_useState47, 2),
    otherStartAge = _useState48[0],
    setOtherStartAge = _useState48[1];
  var _useState49 = useState((_saved$otherEndAge = saved.otherEndAge) !== null && _saved$otherEndAge !== void 0 ? _saved$otherEndAge : MOBILE_DEFAULTS.otherEndAge),
    _useState50 = _slicedToArray(_useState49, 2),
    otherEndAge = _useState50[0],
    setOtherEndAge = _useState50[1];
  var _useState51 = useState((_saved$otherCola = saved.otherCola) !== null && _saved$otherCola !== void 0 ? _saved$otherCola : MOBILE_DEFAULTS.otherCola),
    _useState52 = _slicedToArray(_useState51, 2),
    otherCola = _useState52[0],
    setOtherCola = _useState52[1];
  var _useState53 = useState(false),
    _useState54 = _slicedToArray(_useState53, 2),
    showDetails = _useState54[0],
    setShowDetails = _useState54[1];

  // Auto-save all inputs to localStorage (debounced) whenever any of them change, so the app
  // remembers your parameters between visits. showDetails is a view toggle, not a parameter,
  // so it's intentionally excluded.
  useEffect(function () {
    var t = setTimeout(function () {
      return saveMobileState({
        currentAge: currentAge,
        retirementAge: retirementAge,
        legacyAge: legacyAge,
        portfolio: portfolio,
        annualContribution: annualContribution,
        desiredSpending: desiredSpending,
        contributionMode: contributionMode,
        currentSalary: currentSalary,
        contributionPercent: contributionPercent,
        cagr: cagr,
        inflationRate: inflationRate,
        ssEnabled: ssEnabled,
        ssMonthly: ssMonthly,
        ssClaimAge: ssClaimAge,
        ssCola: ssCola,
        pensionEnabled: pensionEnabled,
        pensionAnnual: pensionAnnual,
        pensionStartAge: pensionStartAge,
        pensionCola: pensionCola,
        otherEnabled: otherEnabled,
        otherAnnual: otherAnnual,
        otherStartAge: otherStartAge,
        otherEndAge: otherEndAge,
        otherCola: otherCola
      });
    }, SAVE_DEBOUNCE_MS);
    return function () {
      return clearTimeout(t);
    };
  }, [currentAge, retirementAge, legacyAge, portfolio, annualContribution, desiredSpending, contributionMode, currentSalary, contributionPercent, cagr, inflationRate, ssEnabled, ssMonthly, ssClaimAge, ssCola, pensionEnabled, pensionAnnual, pensionStartAge, pensionCola, otherEnabled, otherAnnual, otherStartAge, otherEndAge, otherCola]);

  // Still the example until any input moves off it.
  var isExample = Object.keys(MOBILE_DEFAULTS).every(function (k) {
    return {
      currentAge: currentAge,
      retirementAge: retirementAge,
      legacyAge: legacyAge,
      portfolio: portfolio,
      annualContribution: annualContribution,
      contributionMode: contributionMode,
      currentSalary: currentSalary,
      contributionPercent: contributionPercent,
      desiredSpending: desiredSpending,
      cagr: cagr,
      inflationRate: inflationRate,
      ssEnabled: ssEnabled,
      ssMonthly: ssMonthly,
      ssClaimAge: ssClaimAge,
      ssCola: ssCola,
      pensionEnabled: pensionEnabled,
      pensionAnnual: pensionAnnual,
      pensionStartAge: pensionStartAge,
      pensionCola: pensionCola,
      otherEnabled: otherEnabled,
      otherAnnual: otherAnnual,
      otherStartAge: otherStartAge,
      otherEndAge: otherEndAge,
      otherCola: otherCola
    }[k] === MOBILE_DEFAULTS[k];
  });
  var birthYear = new Date().getFullYear() - currentAge;

  // === Run the engine ===
  // Build minimal personalInfo/accounts/streams from the simple inputs.
  // Use useMemo so calc only re-runs when inputs change.
  var _useMemo = useMemo(function () {
      var pi = {
        myAge: currentAge,
        myBirthYear: birthYear,
        myRetirementAge: retirementAge,
        myLifeExpectancy: legacyAge,
        legacyAge: legacyAge,
        filingStatus: 'single',
        state: 'Alabama',
        // Engine keys on full state names; 'CA' silently fell through to 0% tax
        currentExpenses: desiredSpending,
        desiredRetirementIncome: desiredSpending,
        inflationRate: inflationRate / 100,
        survivorModelEnabled: false,
        survivorSpendingFactor: 0.75,
        rothConversionAmount: 0,
        rothConversionBracket: '',
        rothConversionStartAge: 0,
        rothConversionEndAge: 0,
        charitableGivingPercent: 0,
        healthcareModel: 'none',
        // Engine reads pi.healthcareModel (enum). The old healthcareModelEnabled key was ignored, so healthcare costs were billed by default on mobile (B11).
        // Stated rather than inferred: long-term care is its own switch in the
        // engine, and the bracket-fill withdrawal order is off. This gut-check
        // has no controls for either, so neither may quietly bill or reorder.
        ltcModel: 'none',
        withdrawalBracketFill: ''
      };
      // In percent mode the engine reads owner salary from the earned_income stream we inject
      // below and computes contribution = salary × employeePercent each year (with salary COLA).
      // In fixed mode we pass the $ amount and let it stay flat in nominal $.
      var isPercent = contributionMode === 'percent';
      var accounts = [isPercent ? {
        id: 1,
        name: 'Portfolio',
        type: '401k',
        balance: portfolio,
        contributionMode: 'percent',
        employeePercent: contributionPercent / 100,
        employerMatchPercent: 0,
        cagr: cagr / 100,
        startAge: currentAge,
        stopAge: retirementAge,
        owner: 'me'
      } : {
        id: 1,
        name: 'Portfolio',
        type: '401k',
        balance: portfolio,
        contribution: annualContribution,
        contributionGrowth: 0,
        cagr: cagr / 100,
        startAge: currentAge,
        stopAge: retirementAge,
        owner: 'me'
      }];

      // Build the income streams array based on which are enabled.
      // The COLA toggle controls whether the stream inflates year-over-year:
      //   - COLA on:  stream uses 2.5% (SS-like) for SS, or matches user inflationRate for others.
      //                Since the engine treats `cola` as a fixed rate, we set it to the user's inflation
      //                rate so the stream maintains real purchasing power. Special case for SS: it has
      //                its own COLA tradition close to but not identical to CPI; we use the same rate
      //                as inflation for simplicity rather than introducing a separate slider.
      //   - COLA off: cola = 0, meaning the stream is fixed in nominal dollars and shrinks in real terms.
      var inflationFraction = inflationRate / 100;
      var streams = [];

      // In percent mode the engine needs an earned_income stream to read myEarnedIncome from.
      // Salary grows at the user's inflation rate (close enough to typical wage COLA for this
      // mobile what-if). endAge = retirementAge - 1 so the stream ends the year before retirement,
      // matching the account's stopAge (which is exclusive in the engine's contribution loop).
      if (isPercent && currentSalary > 0) {
        streams.push({
          id: 9,
          type: 'earned_income',
          owner: 'me',
          amount: currentSalary,
          cola: inflationFraction,
          startAge: currentAge,
          endAge: Math.max(currentAge, retirementAge - 1)
        });
      }
      var annualSS = 0;
      if (ssEnabled && ssMonthly > 0) {
        annualSS = calculateSSBenefit(ssMonthly, ssClaimAge, birthYear) * 12;
        if (annualSS > 0) {
          streams.push({
            id: 10,
            type: 'social_security',
            owner: 'me',
            amount: annualSS,
            cola: ssCola ? inflationFraction : 0,
            startAge: ssClaimAge,
            endAge: legacyAge,
            pia: ssMonthly
          });
        }
      }

      // Pension: treat as the engine's "pension" type — fully taxable ordinary income, no FICA.
      if (pensionEnabled && pensionAnnual > 0) {
        streams.push({
          id: 11,
          type: 'pension',
          owner: 'me',
          amount: pensionAnnual,
          cola: pensionCola ? inflationFraction : 0,
          startAge: pensionStartAge,
          endAge: legacyAge
        });
      }

      // Other: also use "pension" type for tax treatment (ordinary income, no FICA).
      // This is conservative — slightly overstates tax for rentals (which often have depreciation),
      // slightly understates for part-time work (no FICA modeled). Mobile tool, simplification accepted.
      if (otherEnabled && otherAnnual > 0) {
        streams.push({
          id: 12,
          type: 'pension',
          owner: 'me',
          amount: otherAnnual,
          cola: otherCola ? inflationFraction : 0,
          startAge: otherStartAge,
          endAge: otherEndAge
        });
      }
      var proj = [];
      try {
        proj = computeProjections(pi, accounts, streams, [], [], []);
      } catch (e) {
        console.error('Projection error:', e);
        return {
          projections: [],
          stressCtx: null,
          retirementProj: null,
          legacyProj: null,
          sustainableAnnual: 0,
          ssAnnual: 0,
          pensionFirstYear: 0,
          otherFirstYear: 0,
          guaranteedAtRetirement: 0
        };
      }
      var retirementProj = proj.find(function (p) {
        return p.myAge === retirementAge;
      }) || null;
      var legacyProj = proj.find(function (p) {
        return p.myAge === legacyAge;
      }) || null;

      // Guaranteed income at retirement = sum of all streams active in that year (nominal $$)
      var guaranteedAtRetirement = Math.round(((retirementProj === null || retirementProj === void 0 ? void 0 : retirementProj.socialSecurity) || 0) + ((retirementProj === null || retirementProj === void 0 ? void 0 : retirementProj.pension) || 0));

      // Sustainable annual spending = 4% of portfolio at retirement + guaranteed income at retirement
      var safe4 = ((retirementProj === null || retirementProj === void 0 ? void 0 : retirementProj.totalPortfolio) || 0) * 0.04;
      var sustainableAnnual = Math.round(safe4 + guaranteedAtRetirement);

      // First-year values (today's-dollar equivalents): grab the projection at each stream's start age
      // to show users what their stream actually pays out when it begins. Then deflate back to today's $.
      // Actually simpler: show the user the today's-dollar values they entered, since those are easier
      // to reason about. The nominal future values are visible in the details panel via the engine's output.
      var pensionFirstYear = pensionEnabled ? Math.round(pensionAnnual) : 0;
      var otherFirstYear = otherEnabled ? Math.round(otherAnnual) : 0;
      return {
        projections: proj,
        // The same inputs the projection above was built from, in the shape the
        // engine's bisection expects. Assembled here rather than in the panel so a
        // stress run and the headline projection can never be describing two
        // different plans.
        stressCtx: {
          pi: pi,
          accts: accounts,
          streams: streams,
          assetList: [],
          events: [],
          recurring: []
        },
        retirementProj: retirementProj,
        legacyProj: legacyProj,
        sustainableAnnual: sustainableAnnual,
        ssAnnual: Math.round(annualSS),
        pensionFirstYear: pensionFirstYear,
        otherFirstYear: otherFirstYear,
        guaranteedAtRetirement: guaranteedAtRetirement
      };
    }, [currentAge, retirementAge, legacyAge, portfolio, annualContribution, desiredSpending, cagr, inflationRate, ssEnabled, ssMonthly, ssClaimAge, ssCola, pensionEnabled, pensionAnnual, pensionStartAge, pensionCola, otherEnabled, otherAnnual, otherStartAge, otherEndAge, otherCola, birthYear]),
    projections = _useMemo.projections,
    stressCtx = _useMemo.stressCtx,
    retirementProj = _useMemo.retirementProj,
    legacyProj = _useMemo.legacyProj,
    sustainableAnnual = _useMemo.sustainableAnnual,
    ssAnnual = _useMemo.ssAnnual,
    pensionFirstYear = _useMemo.pensionFirstYear,
    otherFirstYear = _useMemo.otherFirstYear,
    guaranteedAtRetirement = _useMemo.guaranteedAtRetirement;

  // Find depletion age (first year after retirement where portfolio hits 0)
  var depletionAge = useMemo(function () {
    if (!projections.length) return null;
    var dep = projections.find(function (p) {
      return p.myAge >= retirementAge && p.totalPortfolio <= 0;
    });
    return dep ? dep.myAge : null;
  }, [projections, retirementAge]);
  var _useState55 = useState(function () {
      try {
        return document.documentElement.getAttribute('data-theme') === 'light' ? 'light' : 'dark';
      } catch (e) {
        return 'dark';
      }
    }),
    _useState56 = _slicedToArray(_useState55, 2),
    themeMode = _useState56[0],
    setThemeMode = _useState56[1];
  var yearsToRetirement = Math.max(0, retirementAge - currentAge);
  var planSurvives = depletionAge === null || depletionAge > legacyAge;
  var surplusAtLegacy = (legacyProj === null || legacyProj === void 0 ? void 0 : legacyProj.totalPortfolio) || 0;
  return /*#__PURE__*/React.createElement("div", {
    className: "min-h-screen bg-slate-950 text-slate-100",
    style: {
      paddingBottom: 'env(safe-area-inset-bottom)'
    }
  }, /*#__PURE__*/React.createElement("header", {
    className: "bg-slate-900 border-b border-slate-800 px-4 py-3 sticky top-0 z-10",
    style: {
      paddingTop: 'max(0.75rem, env(safe-area-inset-top))'
    }
  }, /*#__PURE__*/React.createElement("div", {
    className: "flex items-baseline justify-between gap-2"
  }, /*#__PURE__*/React.createElement("h1", {
    className: "text-lg font-bold text-slate-100"
  }, "My Retirement Plan"), /*#__PURE__*/React.createElement("div", {
    className: "flex items-center gap-2"
  }, /*#__PURE__*/React.createElement("button", {
    onClick: function onClick() {
      var next = themeMode === 'dark' ? 'light' : 'dark';
      setThemeMode(next);
      try {
        if (next === 'light') document.documentElement.setAttribute('data-theme', 'light');else document.documentElement.removeAttribute('data-theme');
        localStorage.setItem('retirement_planner_theme', next);
      } catch (e) {/* storage unavailable — the in-memory switch still holds */}
    },
    className: "text-sm px-2 py-0.5 rounded text-slate-400 active:bg-slate-800",
    "aria-label": themeMode === 'dark' ? 'Switch to light mode' : 'Switch to dark mode'
  }, themeMode === 'dark' ? '☀️' : '🌙'), /*#__PURE__*/React.createElement("span", {
    className: "text-[10px] text-slate-600 tabular-nums"
  }, "v", typeof window !== 'undefined' && window.APP_VERSION ? window.APP_VERSION : 'dev'))), /*#__PURE__*/React.createElement("p", {
    className: "text-xs text-slate-500"
  }, "Your numbers, through the same engine as the full version")), isExample && /*#__PURE__*/React.createElement("div", {
    className: "mx-4 mt-3 px-3 py-2 rounded-lg border border-sky-700/50 bg-sky-900/20 text-xs text-sky-200 leading-relaxed"
  }, "These are example numbers. Replace them with yours below \u2014 the results update as you go, and everything stays on this phone."), /*#__PURE__*/React.createElement("section", {
    className: "px-4 py-3 bg-slate-900/50 border-b border-slate-800"
  }, /*#__PURE__*/React.createElement("div", {
    className: "grid grid-cols-2 gap-2 mb-3"
  }, /*#__PURE__*/React.createElement(ResultCard, {
    label: "You retire in",
    value: yearsToRetirement === 0 ? 'Now' : "".concat(yearsToRetirement, " yr").concat(yearsToRetirement === 1 ? '' : 's'),
    sublabel: "Age ".concat(retirementAge),
    color: "sky"
  }), /*#__PURE__*/React.createElement(ResultCard, {
    label: "Portfolio at retirement",
    value: fmt(retirementProj === null || retirementProj === void 0 ? void 0 : retirementProj.totalPortfolio),
    sublabel: "Inflation-adjusted up",
    color: "emerald"
  }), /*#__PURE__*/React.createElement(ResultCard, {
    label: "Sustainable spending",
    value: fmt(sustainableAnnual),
    sublabel: "4% portfolio + income",
    color: "amber"
  }), /*#__PURE__*/React.createElement(ResultCard, {
    label: planSurvives ? "Cushion at ".concat(legacyAge) : 'Portfolio runs out',
    value: planSurvives ? fmt(surplusAtLegacy) : "Age ".concat(depletionAge),
    sublabel: planSurvives ? 'Money outlives you' : 'Plan may fail',
    color: planSurvives ? 'emerald' : 'red'
  })), /*#__PURE__*/React.createElement("div", {
    className: "text-center text-xs font-medium py-1.5 px-3 rounded-full ".concat(planSurvives ? 'bg-emerald-900/40 text-emerald-300' : 'bg-red-900/40 text-red-300')
  }, planSurvives ? "\u2713 Sustainable through age ".concat(legacyAge) : "\u26A0 Portfolio depletes at age ".concat(depletionAge, " (before age ").concat(legacyAge, ")")), projections.length > 0 && /*#__PURE__*/React.createElement("div", {
    className: "mt-3"
  }, /*#__PURE__*/React.createElement(Sparkline, {
    data: projections,
    retirementAge: retirementAge
  }), /*#__PURE__*/React.createElement("div", {
    className: "flex justify-between text-[10px] text-slate-500 mt-0.5"
  }, /*#__PURE__*/React.createElement("span", null, "Age ", currentAge), /*#__PURE__*/React.createElement("span", {
    className: "text-amber-500"
  }, "\u2191 Retire ", retirementAge), /*#__PURE__*/React.createElement("span", null, "Age ", legacyAge)))), /*#__PURE__*/React.createElement("section", {
    className: "px-4 py-3 space-y-1"
  }, /*#__PURE__*/React.createElement("h2", {
    className: "text-xs font-semibold text-slate-500 uppercase tracking-wider mb-1"
  }, "Your Situation"), /*#__PURE__*/React.createElement(SliderRow, {
    label: "Your age now",
    value: currentAge,
    onChange: setCurrentAge,
    min: 18,
    max: 75
  }), /*#__PURE__*/React.createElement(SliderRow, {
    label: "Retirement age",
    value: retirementAge,
    onChange: setRetirementAge,
    min: Math.max(currentAge, 50),
    max: 75
  }), /*#__PURE__*/React.createElement(SliderRow, {
    label: "Plan through age",
    value: legacyAge,
    onChange: setLegacyAge,
    min: 75,
    max: 100
  }), /*#__PURE__*/React.createElement(MoneyInput, {
    label: "Current portfolio total",
    value: portfolio,
    onChange: setPortfolio,
    hint: "All your retirement & investment accounts combined"
  }), /*#__PURE__*/React.createElement("div", {
    className: "py-2"
  }, /*#__PURE__*/React.createElement("div", {
    className: "flex items-baseline justify-between mb-1.5"
  }, /*#__PURE__*/React.createElement("span", {
    className: "text-sm text-slate-300 font-medium"
  }, "Contribution mode")), /*#__PURE__*/React.createElement("div", {
    className: "flex gap-2"
  }, /*#__PURE__*/React.createElement("button", {
    type: "button",
    onClick: function onClick() {
      return setContributionMode('fixed');
    },
    className: "flex-1 px-3 py-2 rounded-lg text-sm font-medium border transition-colors ".concat(contributionMode !== 'percent' ? 'bg-emerald-600/30 text-emerald-300 border-emerald-500/60' : 'bg-slate-800 text-slate-400 border-slate-700 hover:text-slate-200')
  }, "$ Fixed"), /*#__PURE__*/React.createElement("button", {
    type: "button",
    onClick: function onClick() {
      return setContributionMode('percent');
    },
    className: "flex-1 px-3 py-2 rounded-lg text-sm font-medium border transition-colors ".concat(contributionMode === 'percent' ? 'bg-emerald-600/30 text-emerald-300 border-emerald-500/60' : 'bg-slate-800 text-slate-400 border-slate-700 hover:text-slate-200')
  }, "% of salary"))), contributionMode === 'percent' ? /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement(MoneyInput, {
    label: "Current annual salary",
    value: currentSalary,
    onChange: setCurrentSalary,
    hint: "Gross annual pay. Used to compute your $ contribution each year."
  }), /*#__PURE__*/React.createElement(SliderRow, {
    label: "Contribution % (you + employer)",
    value: contributionPercent,
    onChange: setContributionPercent,
    min: 0,
    max: 25,
    step: 0.5,
    format: function format(v) {
      return "".concat(v, "%");
    }
  }), /*#__PURE__*/React.createElement("p", {
    className: "text-[11px] text-emerald-400/80 -mt-1 mb-2 px-1 tabular-nums"
  }, "Year 1: $", Math.round(currentSalary * contributionPercent / 100).toLocaleString(), "/yr", ' → ', "Year ", Math.max(1, retirementAge - currentAge), ": $", Math.round(currentSalary * Math.pow(1 + inflationRate / 100, Math.max(0, retirementAge - currentAge - 1)) * contributionPercent / 100).toLocaleString(), "/yr")) : /*#__PURE__*/React.createElement(MoneyInput, {
    label: "Annual contributions",
    value: annualContribution,
    onChange: setAnnualContribution,
    hint: "What you save each year (yours + employer match)"
  }), /*#__PURE__*/React.createElement(MoneyInput, {
    label: "Desired annual spending",
    value: desiredSpending,
    onChange: setDesiredSpending,
    hint: "What you want to spend each year in retirement (today's dollars)"
  }), /*#__PURE__*/React.createElement("h2", {
    className: "text-xs font-semibold text-slate-500 uppercase tracking-wider mb-1 mt-4"
  }, "Assumptions"), /*#__PURE__*/React.createElement(SliderRow, {
    label: "Investment return (CAGR)",
    value: cagr,
    onChange: setCagr,
    min: 2,
    max: 12,
    step: 0.5,
    format: function format(v) {
      return "".concat(v, "%");
    }
  }), /*#__PURE__*/React.createElement(SliderRow, {
    label: "Inflation rate",
    value: inflationRate,
    onChange: setInflationRate,
    min: 1,
    max: 6,
    step: 0.5,
    format: function format(v) {
      return "".concat(v, "%");
    }
  }), /*#__PURE__*/React.createElement("h2", {
    className: "text-xs font-semibold text-slate-500 uppercase tracking-wider mb-1 mt-4"
  }, "Retirement Income Streams"), /*#__PURE__*/React.createElement("p", {
    className: "text-[11px] text-slate-500 mb-2"
  }, "Income you'll receive in retirement that isn't from your portfolio. Toggle each one on/off and configure."), /*#__PURE__*/React.createElement(IncomeStreamSection, {
    title: "Social Security",
    accentColor: "sky",
    enabled: ssEnabled,
    onToggle: setSsEnabled,
    annualPreview: ssEnabled ? ssAnnual : null
  }, /*#__PURE__*/React.createElement(MoneyInput, {
    label: "Monthly benefit at FRA (PIA)",
    value: ssMonthly,
    onChange: setSsMonthly,
    hint: "See ssa.gov for your PIA estimate"
  }), /*#__PURE__*/React.createElement(SliderRow, {
    label: "Claim age",
    value: ssClaimAge,
    onChange: setSsClaimAge,
    min: 62,
    max: 70,
    format: function format(v) {
      return v === 67 ? '67 (FRA)' : String(v);
    }
  }), /*#__PURE__*/React.createElement(ToggleRow, {
    label: "Inflation-adjusted (COLA)",
    value: ssCola,
    onChange: setSsCola,
    hint: "SS has annual COLA \u2014 usually leave on"
  })), /*#__PURE__*/React.createElement(IncomeStreamSection, {
    title: "Pension",
    accentColor: "violet",
    enabled: pensionEnabled,
    onToggle: setPensionEnabled,
    annualPreview: pensionEnabled ? pensionAnnual : null
  }, /*#__PURE__*/React.createElement(MoneyInput, {
    label: "Annual amount (today's dollars)",
    value: pensionAnnual,
    onChange: setPensionAnnual,
    hint: "Combined annual pension income"
  }), /*#__PURE__*/React.createElement(SliderRow, {
    label: "Start age",
    value: pensionStartAge,
    onChange: setPensionStartAge,
    min: 50,
    max: 75
  }), /*#__PURE__*/React.createElement(ToggleRow, {
    label: "Inflation-adjusted (COLA)",
    value: pensionCola,
    onChange: setPensionCola,
    hint: "Most private pensions are NOT adjusted"
  })), /*#__PURE__*/React.createElement(IncomeStreamSection, {
    title: "Other Income",
    accentColor: "cyan",
    enabled: otherEnabled,
    onToggle: setOtherEnabled,
    annualPreview: otherEnabled ? otherAnnual : null
  }, /*#__PURE__*/React.createElement(MoneyInput, {
    label: "Annual amount (today's dollars)",
    value: otherAnnual,
    onChange: setOtherAnnual,
    hint: "Rental, business, royalties, part-time work, etc."
  }), /*#__PURE__*/React.createElement(SliderRow, {
    label: "Start age",
    value: otherStartAge,
    onChange: setOtherStartAge,
    min: 50,
    max: 75
  }), /*#__PURE__*/React.createElement(SliderRow, {
    label: "End age",
    value: otherEndAge,
    onChange: setOtherEndAge,
    min: Math.max(otherStartAge, 60),
    max: 100
  }), /*#__PURE__*/React.createElement(ToggleRow, {
    label: "Inflation-adjusted",
    value: otherCola,
    onChange: setOtherCola,
    hint: "Rentals/royalties usually inflate; business income varies"
  })), (ssEnabled || pensionEnabled || otherEnabled) && /*#__PURE__*/React.createElement("p", {
    className: "text-xs text-slate-400 mt-1 px-2"
  }, "Total guaranteed income at retirement (age ", retirementAge, "): ", /*#__PURE__*/React.createElement("span", {
    className: "text-emerald-400 font-medium"
  }, fmtFull(guaranteedAtRetirement), "/yr")), /*#__PURE__*/React.createElement("button", {
    onClick: function onClick() {
      return setShowDetails(!showDetails);
    },
    className: "w-full mt-6 py-2.5 text-sm text-slate-400 border border-slate-700 rounded-lg hover:bg-slate-800 transition-colors"
  }, showDetails ? '▲ Hide details' : '▼ Show plan details'), stressCtx && projections.length > 0 && /*#__PURE__*/React.createElement(BreakingPointPanel, {
    ctx: stressCtx,
    projections: projections,
    retirementAge: retirementAge
  }), showDetails && retirementProj && legacyProj && /*#__PURE__*/React.createElement("div", {
    className: "mt-3 space-y-2 text-sm bg-slate-800/40 border border-slate-700 rounded-lg p-3"
  }, /*#__PURE__*/React.createElement("div", {
    className: "flex justify-between"
  }, /*#__PURE__*/React.createElement("span", {
    className: "text-slate-400"
  }, "Years until retirement"), /*#__PURE__*/React.createElement("span", {
    className: "text-slate-200 tabular-nums"
  }, yearsToRetirement)), /*#__PURE__*/React.createElement("div", {
    className: "flex justify-between"
  }, /*#__PURE__*/React.createElement("span", {
    className: "text-slate-400"
  }, "Total contributions"), /*#__PURE__*/React.createElement("span", {
    className: "text-slate-200 tabular-nums"
  }, fmtFull(contributionMode === 'percent' ? projections.filter(function (p) {
    return p.myAge < retirementAge;
  }).reduce(function (s, p) {
    return s + (p.perAccountContributions && p.perAccountContributions[1] || 0);
  }, 0) : annualContribution * yearsToRetirement))), /*#__PURE__*/React.createElement("div", {
    className: "flex justify-between"
  }, /*#__PURE__*/React.createElement("span", {
    className: "text-slate-400"
  }, "Portfolio at retirement"), /*#__PURE__*/React.createElement("span", {
    className: "text-emerald-300 tabular-nums"
  }, fmtFull(retirementProj.totalPortfolio))), ssEnabled && ssAnnual > 0 && /*#__PURE__*/React.createElement("div", {
    className: "flex justify-between"
  }, /*#__PURE__*/React.createElement("span", {
    className: "text-slate-400"
  }, "Lifetime SS received"), /*#__PURE__*/React.createElement("span", {
    className: "text-sky-300 tabular-nums"
  }, fmtFull(projections.filter(function (p) {
    return p.myAge >= ssClaimAge;
  }).reduce(function (s, p) {
    return s + (p.socialSecurity || 0);
  }, 0)))), (pensionEnabled || otherEnabled) && /*#__PURE__*/React.createElement("div", {
    className: "flex justify-between"
  }, /*#__PURE__*/React.createElement("span", {
    className: "text-slate-400"
  }, "Lifetime pension + other"), /*#__PURE__*/React.createElement("span", {
    className: "text-violet-300 tabular-nums"
  }, fmtFull(projections.reduce(function (s, p) {
    return s + (p.pension || 0);
  }, 0)))), /*#__PURE__*/React.createElement("div", {
    className: "flex justify-between"
  }, /*#__PURE__*/React.createElement("span", {
    className: "text-slate-400"
  }, "Lifetime taxes paid"), /*#__PURE__*/React.createElement("span", {
    className: "text-purple-300 tabular-nums"
  }, fmtFull(projections.filter(function (p) {
    return p.myAge >= retirementAge;
  }).reduce(function (s, p) {
    return s + (p.totalTax || 0);
  }, 0)))), /*#__PURE__*/React.createElement("div", {
    className: "flex justify-between"
  }, /*#__PURE__*/React.createElement("span", {
    className: "text-slate-400"
  }, "Net worth at age ", legacyAge), /*#__PURE__*/React.createElement("span", {
    className: "text-emerald-300 tabular-nums"
  }, fmtFull(legacyProj.totalPortfolio)))), /*#__PURE__*/React.createElement("p", {
    className: "text-[11px] text-slate-600 mt-6 px-2 leading-relaxed text-center"
  }, "Simplified single-filer model. The survivor-tax and Roth-conversion reports are desktop-only \u2014 both are about inputs this model doesn't collect (a spouse, and separate pre-tax/Roth balances). For those, plus multi-account, IRMAA and full tax analysis, use the desktop version. Educational tool \u2014 not financial advice."), /*#__PURE__*/React.createElement("p", {
    className: "text-center mt-3 mb-2"
  }, /*#__PURE__*/React.createElement("a", {
    href: "index.html?desktop=1",
    className: "text-xs text-slate-500 underline decoration-dotted underline-offset-2 hover:text-slate-300"
  }, "Switch to full desktop version \u2192"))));
}